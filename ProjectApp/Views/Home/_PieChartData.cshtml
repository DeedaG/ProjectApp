@*@model Dictionary<System.String, System.Int32>*@
@*@model IEnumerable<ProjectApp.Models.ProjectViewModel>*@
@model dynamic


<script type="text/javascript" src="https://www.gstatic.com/charts/loader.js"></script>
<script type="text/javascript">
google.charts.load('current', { 'packages': ['corechart'] });

    google.charts.setOnLoadCallback(drawChart);

    function drawChart() {

        var data = google.visualization.arrayToDataTable([
            ['Project Language', 'Frequency'],
            @*["C#", @Model.resultData["C#"]],*@
            @*["Ruby", @Model.resultData["Ruby"]],*@
        @*["React", @Model.resultData["React"]],*@

            @*["@Model.projects[@Model.projects.Count - 7].Language", @Model.resultData[@Model.projects[@Model.projects.Count - 7].Language]],*@

            ["@Model.projects[@Model.projects.Count - 6].Language", @Model.resultData[@Model.projects[@Model.projects.Count - 6].Language]],

            ["@Model.projects[@Model.projects.Count - 5].Language", @Model.resultData[@Model.projects[@Model.projects.Count - 5].Language]],

            ["@Model.projects[@Model.projects.Count - 4].Language", @Model.resultData[@Model.projects[@Model.projects.Count - 4].Language]],

            ["@Model.projects[@Model.projects.Count - 3].Language", @Model.resultData[@Model.projects[@Model.projects.Count - 3].Language]],

            ["@Model.projects[@Model.projects.Count - 2].Language", @Model.resultData[@Model.projects[@Model.projects.Count - 2].Language]],

            ["@Model.projects[@Model.projects.Count - 1].Language", @Model.resultData[@Model.projects[@Model.projects.Count - 1].Language]]
        ]);

        @*data.AddColumn('Project Language', 'Frequency');*@
        @*data.AddRows([["@Model.projects[@Model.projects.Count - 1].Language", @Model.resultData[@Model.projects[@Model.projects.Count - 1].Language]]]);*@

        


        var data2 = google.visualization.arrayToDataTable([
            ['Language', 'Days Coding'],
            ["Ruby", 1],
            ["C#", 2],
            ["React", 1]
        ]);


        var data3 = google.visualization.arrayToDataTable([

            ['Name', 'Start Date'],

            ['NewTest1', 8 / 2 / 2020],

            ['Test2', 7 / 1 / 2020],

            ['test3', 8 / 31 / 2020]
        ]);

         var options = {
            backgroundColor: 'transparent',
            title: 'Languages',
            titleTextStyle: {
                        style: "float:right;width:60%"
            },
            slices: [{ color: 'darkorange' }, { color: 'teal' }, { color: 'green' }, { color: 'navy' }, { color: 'purple' }, { color: 'yellow' }, ]
        };

            var options2 = {
                backgroundColor: 'transparent',
                title: 'Development Time',
                titleTextStyle: {
            style: "float:right;width:60%"
            },
            slices: [{ color: 'darkorange' }, { color: 'teal' }, { color: 'green' }, { color: 'navy' }, { color: 'purple' }, { color: 'yellow' },]
        };

            var options3 = {
            backgroundColor: 'transparent',
            title: 'Unfinished Projects',
            titleTextStyle: {
            style: "float:left;width:30%"
            },
            slices: [{ color: 'darkorange' }, { color: 'teal' }, { color: 'green' }, { color: 'navy' }, { color: 'purple' }, { color: 'yellow' },]
        };

            var chart = new google.visualization.PieChart(document.getElementById('piechart'));

            chart.draw(data, options);

            var chart2 = new google.visualization.PieChart(document.getElementById('piechart2'));
            chart2.draw(data2, options2);

            var chart3 = new google.visualization.PieChart(document.getElementById('piechart3'));

            chart3.draw(data3, options3);
    }</script>


